import React from 'react';
import { ListItem, ListItemText } from '@mui/material';
import { Routes, Route } from "react-router-dom";

const Symbologie = () => {
  // Funktion, die aufgerufen wird, wenn auf "Symbologie" geklickt wird

  return (
    <div>Neue Page</div>
  );
};

export default Symbologie;